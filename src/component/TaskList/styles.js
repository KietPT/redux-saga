import { makeStyles } from '@material-ui/core/styles';
const styles = makeStyles({
    root: {
      maxWidth: 300,
      border : "solid"
    },
    media: {
      height: 140,
    },
});

export default styles